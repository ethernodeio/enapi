import { SolCompile } from "../__GENERATED_TYPES__/index.js";

// tslint:disable-next-line: variable-name
export const sol_compile: SolCompile = async (u) => {
  return new Promise((resolve, reject) => {
    resolve({
      solResult: "Coming Soon",
    });
  });
};
